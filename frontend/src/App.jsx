import ConnectionCheck from "./pages/ConnectionCheck.jsx";

/**
 * De momento una sola pantalla y sin router.
 *
 * El router entra en la entrega 6, cuando haya una segunda pantalla que
 * enrutar. Montarlo ahora sería una capa que solo sirve para verse a sí misma.
 */
export default function App() {
  return <ConnectionCheck />;
}
